import React, { memo } from 'react'
import { OverInfoWrapper } from './style'

const OverInfo = memo(() => {
  return (
    <OverInfoWrapper>OverInfo</OverInfoWrapper>
  )
})

export default OverInfo